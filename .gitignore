node_modules/
backups/
_query_*.ps1
_migrate_*.ps1
# Generated locally by `npm run migrate` (rebuilt from inventory.accdb / tracker_data.json)
inventory.db-wal
inventory.db-shm
*.log
*.png
*.json-bak
temp/
.idea/
.vscode/
.gemini/
*.tmp
mail.config.json

# Runtime database + redundant dumps must never be committed (contains password
# hashes + live session tokens). Build from tracker_data.json via `npm run migrate`.
inventory.db
inventory.accdb
test_output.json
